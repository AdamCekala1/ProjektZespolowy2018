import { DictionaryToArrayPipe } from './dictionary-to-array.pipe';

describe('DictionaryToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
